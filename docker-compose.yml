services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-studio-applet
    env_file:
      - .env.docker
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
